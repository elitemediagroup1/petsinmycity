{
  "name": "@petsinmycity/knowledge",
  "version": "0.2.0",
  "private": true,
  "description": "Knowledge Graph storage foundation for PetsInMyCity. Durable, serverless-compatible, versioned, provenance-tracked storage implementing the frozen Machine Schema envelope behind an async KnowledgeStore interface (ADR-0027).",
  "main": "src/index.js",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "load:austin": "node scripts/load-austin.js",
    "readiness": "node scripts/readiness.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "js-yaml": "^4.1.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
