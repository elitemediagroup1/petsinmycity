# Local database artifacts (the store is a rebuildable artifact; YAML is source of record)
data/
*.db
*.db-wal
*.db-shm

# Environment / credentials (NEVER commit real secrets; .env.example is the template)
.env
.env.*
!.env.example

# Dependencies
node_modules/
